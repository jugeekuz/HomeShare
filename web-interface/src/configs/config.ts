const config = {
    MAX_CHUNK_SIZE_MB: 5,
    MAX_CHUNK_RETRIES: 5,
    MAX_CONCURRENT_CHUNKS: 6,
    MAX_FILE_SIZE_MB: 100 * 1024 * 1024,
    UPLOAD_URL: 'http://kuza.gr:8080/upload'
}

export default config